/* globals L describe it expect*/

describe('L.esri', function() {

    // just to get the party started
     it('returns a valid object', function() {
          expect(L.esri).to.be.ok();
     });
});